service : {
  /// * Get the price of a token from a pool
  ///      * @param poolId - The pool id array
  ///      * @return - Array of (token, price)
  get_prices_from_ckusdc_pools: () ->
   (vec record {
          text;
          float64;
        }) composite_query;
}
